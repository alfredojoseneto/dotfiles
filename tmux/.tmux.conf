# ============================================================================
# ~/.tmux.conf — macOS Tahoe edition (adapted from a Debian/Linux config)
# ============================================================================
# IMPORTANT: run `which zsh` in your Terminal and confirm the path below
# matches. Apple Silicon + Homebrew -> /opt/homebrew/bin/zsh
# Intel Mac + Homebrew         -> /usr/local/bin/zsh
# System zsh (no Homebrew)     -> /bin/zsh
# ============================================================================

# -------------------- GENERAL CONFIGURATION ----------------------------------

# add these lines to ~/.zshrc if you want colored prompt detection:
# case "$TERM" in
#     xterm-color|*-256color) color_prompt=yes;;
# esac

# remove escape time delay (important for vim/nvim responsiveness)
set -s -g escape-time 0

# improve terminal colors
# set -g default-terminal "screen-256color"
# set -ga terminal-overrides ",xterm-256color:Tc"

# Undercurl support (needs tmux >= 3.0)
set -as terminal-overrides ',*:Smulx=\E[4::%p1%dm'
set -as terminal-overrides ',*:Setulc=\E[58::2::%p1%{65536}%/%d::%p1%{256}%/%{255}%&%d::%p1%{255}%&%d%;m'

# set tmux to use zsh instead of bash (EDIT THIS PATH — see note above)
set -g default-shell /opt/homebrew/bin/zsh
set -g default-command /opt/homebrew/bin/zsh

# increase scrollback buffer to 10000
set -g history-limit 10000

# setting encoding
# set -g status-utf8 on                  # expect UTF-8 (tmux < 2.2)
# setw -g utf8 on

# -------------------- RELOAD CONFIGURATION -----------------------------------
unbind r
bind r source-file ~/.tmux.conf \; display "Reloaded tmux config!"

# -------------------- DISPLAY CONFIGURATION ----------------------------------
set -g base-index 1           # start windows numbering at 1
setw -g pane-base-index 1     # make pane numbering consistent with windows

setw -g automatic-rename off  # rename window to reflect current program
set -g renumber-windows on    # renumber windows when a window is closed

set -g set-titles on          # set terminal title

set -g display-panes-time 800 # slightly longer pane indicators display time
set -g display-time 1000      # slightly longer status messages display time

# -------------------- NAVIGATION ---------------------------------------------
# Set new panes/windows to open in current directory
bind c new-window -c "#{pane_current_path}"
bind -r s split-window -v -c "#{pane_current_path}"   # horizontal split
bind -r v split-window -h -c "#{pane_current_path}"   # vertical split

# toggle resize panes evenly vertial using "PREFIX ev"
bind -T multiKeyBindings v select-layout even-vertical
bind e switch-client -T multiKeyBindings

# toggle resize panes evenly horizontaly usin "PREFIX eh"
bind -T multiKeyBindings h select-layout even-horizontal
bind e switch-client -T multiKeyBindings

# pane navigation (vim-style)
bind h select-pane -L     # move left
bind j select-pane -D     # move down
bind k select-pane -U     # move up
bind l select-pane -R     # move right
bind > swap-pane -D       # swap current pane with the next one
bind < swap-pane -U       # swap current pane with the previous one

# -------------------- COPY and PASTE (macOS native clipboard) ----------------
# Enable vi-copy-mode
setw -g mode-keys vi

# turn this on if you want to use touchpad/mouse to scroll and select panes
setw -g mouse on

# macOS ships with pbcopy/pbpaste natively — no xclip or extra install needed
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel 'pbcopy'
bind -T copy-mode-vi Enter send-keys -X copy-pipe-and-cancel 'pbcopy'
bind -n -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel 'pbcopy'

# -------------------- SYNCHRONIZE PANES --------------------------------------
bind a setw synchronize-panes \; display "synchronize-panes #{?pane_synchronized,#[bg=red]#[fg=yellow] ON ,#[bg=cyan]#[fg=black]OFF}"

# -------------------- APPEARANCE ---------------------------------------------
set -g status on
set -g status-interval 1
set -g status-position bottom
set -g status-style 'fg=colour6,bg=colour0'
set -g status-left-length 30
set -g status-left ' #S ⟩ '
set -g status-justify left
set -g window-status-format '#[fg=colour8]#I:#W ⟩'
set -g window-status-current-format '#[fg=colour15]#I:#W ⟩'

set -g status-right '⟨ #P:#{window_panes} '

set -g pane-border-style 'fg=colour0'
set -g pane-active-border-style 'bg=default fg=colour6'

# ----------------------------- LIST OF PLUGINS --------------------------------
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'christoomey/vim-tmux-navigator'

# Dracula Theme configuration
set -g @plugin 'dracula/tmux'

# available plugins: battery, cpu-usage, git, gpu-usage, ram-usage, tmux-ram-usage,
# network, network-bandwidth, network-ping, attached-clients, network-vpn, weather,
# time, spotify-tui, kubernetes-context, synchronize-panes
# set -g @dracula-plugins "cpu-usage ram-usage time"
set -g @dracula-plugins "time"
set -g @dracula-show-powerline true
set -g @dracula-show-left-icon session   # accepts: session, smiley, window, or any character
set -g @dracula-refresh-rate 5

# Make sure Homebrew binaries are visible to tmux (adjust order for Intel Macs:
# Intel Homebrew installs to /usr/local/bin, so put it before /opt/homebrew/bin)
# set-environment -g PATH "/opt/homebrew/bin:/bin:/usr/bin:/usr/local/bin"

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
if "test ! -d ~/.tmux/plugins/tpm" \
   "run 'git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm'"
run -b '~/.tmux/plugins/tpm/tpm'
